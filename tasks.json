{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build and Serve",
      "type": "shell",
      "command": "npm run build && serve -s build && start http://localhost:5000",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}
